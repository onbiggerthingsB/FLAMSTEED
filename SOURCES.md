# SOURCES

Per-source provenance, license, refresh cadence, access (free/key/scrape), the
`revision_contaminated` policy, and a `source_version` where pinned. Rows are
**appended per adapter task** (Tasks 4, 8, 9, 10, 13); Task 1 seeds the header,
the FBref/Opta note, and the first (results) row.

| Source | Feature | License | Access | Policy | source_version | Notes |
|---|---|---|---|---|---|---|
| `martj42/international_results` | match results | CC0 | free (git) | point_in_time | `6b6f8e9f321414957cc17861d8c2dbf25c4437b0` (queried 2026-06-09) | immutable results; `valid_as_of == observed_at == match date`. `match_id = sha1(date\|home\|away\|city)`; one real same-day/same-venue double-header (Tahiti–New Caledonia, 1974-02-17) is disambiguated by a deterministic occurrence index so `match_id` is unique. |
| The Odds API (`the-odds-api.com`) | closing 1X2 odds (h2h) | proprietary ToS | **paid key** (live pull **gated** — see Phase-0 decision 1) | point_in_time | — (UNVERIFIED pricing) | CLV benchmarked vs **Pinnacle closing**; historical endpoint returns one timestamped snapshot per call, so we capture a bet-time + near-kickoff `close` series. `valid_as_of == observed_at == snapshot timestamp`. Adapter is **mocked** against `fixtures/oddsapi_historical_sample.json`; `fetch_historical` raises without `api_key` and is never called in tests. Betfair traded-volume/market depth is NOT in this feed (deferred, not fabricated). |
| StatsBomb Open Data (`statsbomb/open-data` via `statsbombpy`) | match-team xG | [user-facing license — non-commercial, attribution required](https://github.com/statsbomb/open-data/blob/master/LICENSE.pdf) | free (no auth; public open data) | point_in_time | `statsbombpy-1.18.0@2026-06-03` | Open Data is **static + append-mostly** (new competitions added over time; existing covered-match data is stable), so for COVERED matches xG is **point-in-time, not revision-contaminated** — `valid_as_of == observed_at == match_date`, "as close as release versioning allows" (no per-pull git tag is exposed by the client, so we pin client version + pull date). xG is **coverage-gated, NULL-safe, NEVER imputed** (uncovered match-team → absent/NULL row, flagged via `xg_covered`; gap enumerated in `coverage.py`). Adapter (`sources/statsbomb.py`): pure `normalize_match_xg` aggregates shot xG → match-team xG; thin `fetch_competitions`/`fetch_matches`/`fetch_shots` are the ONLY network entries (tests run **offline** against `fixtures/statsbomb_*.json`). **Coverage reality (corrected — `competition_international == True`, not the stale `country_name == "International"` filter, which dropped the continental cups):** free international men's-senior xG = the **8 FIFA World Cup finals editions** (1958/62/70/74/86/90/2018/2022) **+ UEFA Euro 2020 & 2024 + Copa America 2024 + African Cup of Nations 2023** — i.e. **12 men's-senior competition-seasons (~78 national teams)**. **NO qualifiers, NO friendlies, no Nations League.** So the qualifier/friendly tail is absent (NULL, never imputed), **but** continental-cup participants — a meaningful slice of mid/lower-tier sides (full AFCON-2023 & Copa-2024 fields) — **ARE** covered; xG is thus still NULL across the entire (qualifier/friendly/Nations-League-heavy) backtest window but available for finals + continental-cup matches. Compounds the FBref/Opta collapse below. Live coverage report (regenerated by `scripts/gen_statsbomb_coverage.py`): `reports/phase1_statsbomb_coverage.md`. **48-team WC-2026 intersection is GATED on `config/tournament_2026.yaml` (Task 13).** |
| Open-Meteo Historical Weather API (`archive-api.open-meteo.com/v1/archive`, ERA5 reanalysis) | daily match-venue weather (mean temp, total precip) | [CC BY 4.0](https://open-meteo.com/en/license) (attribution: "Weather data by Open-Meteo.com") | free (no key; network) | point_in_time | `era5@open-meteo-archive` (reanalysis; no per-pull tag exposed) | We query **only past dates**, and a past date's reanalysis value is **fixed (never revised)**, so weather is point_in_time: `valid_as_of == observed_at == match_date`; no future leakage (we never read a date later than the match). Adapter `sources/climate.py`: thin `fetch_climate(lat, lon, date, cache_dir)` is the ONLY network entry, cached via `cached_pull` (content-addressed by lat/lon/date → re-pull is a no-op). Tests run **offline** and **never call `fetch_climate`**; coords come from `fixtures/venues_sample.csv`. |
| Derived features (`sources/derived.py`) | rest days, travel distance (km), venue altitude (m) | n/a (computed in-repo) | n/a (no fetch) | point_in_time | — (function of fixture+venue inputs) | **point_in_time BY CONSTRUCTION** — pure functions over the immutable fixture schedule + static venue coords, **no network, no store, no future leakage**: `rest_days` is the gap to a team's *prior* fixture (**first match → NaN**); `travel_distance` is the haversine km (Earth R=6371; London→Paris ≈ 343 km) from the *previous* venue (**no prior venue → NaN**); `altitude` is a fixed venue property (**unknown venue → NaN**). Nothing here can be revised after the fact, so `valid_as_of == observed_at == match_date`. Venue coords/altitude staged in `fixtures/venues_sample.csv`. |
| Hand-curated elevation reference (`sources/altitude_ref.py`) | city→elevation (m) + country→accustomed altitude (m); the `accl_alt` acclimatized-altitude covariate input (P2a) | n/a (hand-keyed in-repo from published references) | n/a (**NO geocoding/API**; deterministic) | point_in_time | — (static lookup tables) | **point_in_time + leakage-free BY CONSTRUCTION** — `accl_gap(city, team) = CITY_ELEVATION_M[city] − accustomed_alt(team)` derives ONLY from the match row's own venue city + the team labels (both known pre-kickoff; never revised). A city absent from the table → **NaN → masked, never imputed/geocoded** (the `CovariateTransform` mask + the `accl_alt` missing indicator). **Elevation values** hand-keyed to standard published references (Wikipedia/GeoNames city-elevation entries, cross-checked): CONMEBOL altitude venues — La Paz ~3,640 m (Estadio Hernando Siles), Quito ~2,850 m (Olímpico Atahualpa), Bogotá ~2,640 m (El Campín), Sucre ~2,810 m, Cusco ~3,399 m, Oruro/Cochabamba; other high panel cities — Mexico City ~2,240 m (Azteca), Toluca ~2,660 m, Pachuca/Puebla, Quetzaltenango, Addis Ababa, Asmara, Nairobi, Johannesburg, Sana'a, Calama, Ecuadorian highland cities; the **16 WC-2026 venue cities** straight from `config/tournament_2026.yaml`'s `altitude_m` (Estadio Azteca Mexico City 2,240 m and Estadio Akron Guadalajara/Zapopan 1,675 m are the elevated ones; the other 14 lowland). **`accustomed_alt`** (country base-city altitude) is a documented **modeling choice** (single national number = the dominant home venue: Bolivia→La Paz, Ecuador→Quito, Colombia→Bogotá, Mexico→Mexico City; every other country → lowland default 0.0), an UPPER BOUND on squad habituation — not a fact claim about any individual squad. |
| Squad market values (Transfermarkt) | squad / player market value | **proprietary ToS — UNVERIFIED** (programmatic collection not clearly permitted) | **gated / optional** (no scraper implemented) | `current_only` (would be revision-contaminated) | — (not collected) | **DEFERRED — interface only**, the *first optional feature to revisit* (spec §10.2). `sources/market_values.py` defines the `MarketValueSource` Protocol + a `TransfermarktMarketValues` whose `pull()` raises `NotImplementedError`; **no network, no scraping**. Two reasons to defer: (1) license UNVERIFIED; (2) **revision hazard** — market values are restated over time, so scraping the *present-day* value and stamping it with an old `valid_as_of` would leak that revision past the bitemporal read invariant (north-star §4.2). A correct future implementation needs **point-in-time snapshots**, not current state; until then it is `current_only` and would surface in `revision_contaminated_exposure`. |
| clubelo.com club ratings (free API) | point-in-time **club**-Elo (the P3-v0 `squad_z` anchor input — option (b), results-derived) | **free to use, attribution appreciated** (clubelo.com stated policy; recorded here per binding rule 8) | free (no account/key; `http://api.clubelo.com/<YYYY-MM-DD>`; polite throttling) | point_in_time | snapshots @ `2026-06-10` / `2022-11-20` / `2024-06-14` (accessed 2026-06-10) | **Results-derived** club ratings (computed from club match results, like our own international Elo) → stays inside invariant #4 (market-prior-free; NOT betting odds, NOT Transfermarkt market values). Three **committed verbatim CSV snapshots** under `config/squads/clubelo_{20260610,20221120,20240614}.csv` (cols `Rank,Club,Country,Level,Elo,From,To`), one per tournament cutoff. **Point-in-time:** the `/<date>` route returns each club's Elo as-of that date, reflecting ONLY club results known before it (strictly pre-cutoff; leakage-safe the same way altitude/weather are). Endpoints accessed: `http://api.clubelo.com/2026-06-10`, `/2022-11-20`, `/2024-06-14`. **Known cost = coverage** (dense European leagues, sparse non-European) — ACCEPTED under the `has_squad` mask and reported per-confederation in `reports/squad_z_2026-06-11.md`. Offline + reproducible at read time (build script reads only the committed snapshots; **no run-time network**). |
| Tournament squad lists (Wikipedia + announced-finals pages) | hand-curated `team,player,club` rows → the `squad_z` anchor (P3 v0) | Wikipedia text CC BY-SA 4.0; announced-finals press pages cited per team | free (no account; polite manual fetch; **CLUB is the only join key**, `player` is for the coverage log only) | point_in_time (squad as-of each cutoff) | `config/squads/{wc2026,wc2022,euro2024}.csv` (accessed 2026-06-09…11) | **wc2022** (32 teams) ← Wikipedia "2022 FIFA World Cup squads" (https://en.wikipedia.org/wiki/2022_FIFA_World_Cup_squads). **euro2024** (24) ← Wikipedia "UEFA Euro 2024 squads" (https://en.wikipedia.org/wiki/UEFA_Euro_2024_squads). **wc2026** (the 48-team `config/tournament_2026.yaml` model field — an *illustrative* verified-draw, not the real-world qualifiers): qualified teams ← announced finals squad; illustrative-draw teams that did not qualify ← most-recent senior international call-up (a **modeling choice**, recorded per team in the CSV header + the per-team source URLs below). Each squad CSV carries a `#`-comment provenance header (source URL + access date); per-team wc2026 sources enumerated in the wc2026.csv header. **Never invented:** a team whose squad cannot be found = a coverage-gap (no rows + a note), masked via `has_squad=0`, never fabricated. Club spellings reconciled to clubelo `Club` via the explicit `config/squads/club_aliases.csv` (EXACT-match only, NEVER fuzzy). |

## FBref / Opta advanced data — termination note

FBref advanced data (xG) terminated 2026-01-20 — ToS/contract dispute per Stats
Perform's statement to The Athletic (FBref disputes it); NOT the WC exclusivity
deal. We use StatsBomb Open Data for xG.
