IN-TREE ADVERSARIAL SEED AUDIT OF v2 (run inside the repository at the v2-conformance commit, before the closure round)
verdict: FAIL   seeds_red: 17 / 19   tree_clean: True

SUMMARY
FAIL — do not paste the §8.3 freeze block. Audited the working tree at HEAD 1afd54d (21 commits past f454041; the brief's stated HEAD), against reports/epl_widening_prereg_v2.md as sole law.

SEEDS: 17 of 19 satisfied. Red: (a) thin rule inverted → 33 tests; (b) merge identity control loosened → 1; (b2) ENGINE-level §3.2 control loosened to 1e-4 → 1 (last round's green seed, now red — but see finding 4); (c) delta paired against the corpus → 1; (d) stored-vs-recomputed tautology → 1; (e) `date < C` → `<=` → 1; (f) iv-a sign flip → 1; (g) poison shard dropped → 1; (h) parity.jsonl dropped from MANIFEST_PATHS → 1; (i) E_STAR = 9.5 → 1; (j) both variants — a `require_parity` bypass and an oracle subset → 1 each; (n) estimator joint bootstrap de-paired → 1; (o) provisional smuggled into `sampler_digest` via a new parameter → red by BOTH required tests (signature pin and the equal-digest test), closing last round's green seed; (p) first-fit record relocated by a directory argument → L8 red and `freeze_block` refuses; (r) tally row-digest rebinding disabled → 1. Must-refuse probes both held: (l) the real freeze block appended uncommitted to the prereg leaves `frozen=False`, `files={}`, `why` naming 'no COMMITTED harness-hash table', and `require_harness_freeze` raises; (m) every real-artifact entrance — `assert_may_fit` (three ways), `Engine` (scratch and default dir), `TableRunner`, `ParityRunner`, `run_fits`, `run_table` — refuses unfrozen, the scratch dir stayed empty, `read_only_store` raises `StoreNotBuilt` and creates nothing, `table_cells` never names `build_store`, and after running `--membership`, `--plan` and `--freeze-block` for real the shared store is byte- and mtime-identical.

GREEN (the two blocking findings): (k) making P5's K=200 joint resample per-cell leaves 241/241 passing, because the rule's only test uses zero-variance tallies — and on jittered tallies the two constructions give dissenting 0/fired False vs 4/fired True, i.e. resolved vs UNRESOLVED. (q) deleting every `need_marker` precondition from the generated launch.sh leaves L9 and all 12 launcher tests green, because the committed test matches the marker name in a comment.

NO-SEED: conformance predicates spot-checked behaviourally — L13, L14 and L5 each go red when their target is broken; L9 does not (finding 3) and L1 does not (finding 1, blocking: L1 stays green under the exact defect its scenario names). v2 text vs code constants agree everywhere checked (38 pins: e*, grid, α, bar, tolerance, B/alpha/seeds, MC_BOOT/MC_SEED/5e-5/2σ, K/unanimity seed, SHARDS, schema, 52-path MANIFEST, 85/52/51/47/62/78/820/4240/46/35/16/19, the per-label census, the season split, the whole POWER block including POWER_BAR to 19 digits, RUN_ORDER, the five sequence steps, 26 refusals / 27 classes, the four pinned digests plus realised_config_sha256) and PUBLISHED_POWER equals §6.3's six rows exactly; `freeze_block()` renders in 27.7s with L16 green, so the committed power simulation really does reproduce them. `verify` genuinely re-derives the table gate from rebound tallies rather than re-reading JSON. Protected paths untouched f454041..HEAD (only epl/evwiden.py, epl/tests/test_evwiden.py and the two prereg documents changed). Full suite 1331 passed, 1 skipped in 712.87s; epl/tests/test_evwiden.py alone 241 passed in 157s. `scripts/oa_lock.py` → LOCK VALID.

TREE: byte-identical to 1afd54d at the end — `git status --porcelain`, `git diff --stat` and `git diff 1afd54d --stat` all empty; harness digests 604f4f33… / 61a97f7c…; no data/epl/sim/evwiden*, no evwiden.json, no reports/evidence/widening*, no result document; the shared store untouched at 184,115 bytes, mtime 2026-08-14 18:41. One pre-existing exception, not created by me and reported as finding 6: data/epl/fit/evwiden/first_real_fit.json, dated 2026-08-28 15:34, which §8.8's attestation says cannot exist.

FINDINGS
--- [1] severity=blocking
Conformance row L1 cannot go red, so the freeze gate grades nothing on §2.3's pairing. /Users/likerun/Desktop/worldcup/epl/evwiden.py:7501-7504 reads `l1 = (A and B) or C` with `C = float(made["rps_B"]) == float(made["rps_native"])`. The row's own fixture sets `probs_incumbent` equal to the corpus row's `dc_home/dc_draw/dc_away`, so C is true by construction — and A-and-B is *always* false there, because rps_B == rps_native forces delta == delta_vs_corpus. PROVED: with the estimand's delta seeded to `rps_arm - rps_native` (exactly the defect L1's scenario names — 'rewire the enlarged pass to read the corpus'), `implementation_report` returns L1 ok=True with detail delta 0.06875 == delta_vs_corpus 0.06875. §8.5: 'A row that cannot go red is not a row', and `--freeze-block` gates on the report — which today renders 18/18 green and would be pasted into the freeze commit. L1 is also not one of the seven rows §8.5 makes the test re-execute independently, so nothing else covers it. Fix: drop the `or` disjunct and give the row a fixture whose Arm B differs from the corpus.

--- [2] severity=blocking
§5.4's P5 joint particle resample can be made per-cell with the whole suite green, and it flips gate (iv) between resolved and UNRESOLVED. Seeding /Users/likerun/Desktop/worldcup/epl/evwiden.py:5930-5934 (`unanimity`) to draw one `picked` per cell instead of one per replicate applied to all 32 tallies: 241 passed. This is the brief's own seed (k) wording ('K resamples skipped or made per-cell') and §10's invalidation ('an MC estimator that is not §5's jointly-resampled, tie-aware estimator'). Cause: the only test of the rule, `test_the_unanimity_rule_propagates_through_the_actual_computation` (/Users/likerun/Desktop/worldcup/epl/tests/test_evwiden.py:3233-3273), builds its cells with `jitter=0` — zero-variance tallies, under which every resample is identical and the joint/per-cell distinction is invisible; its own docstring says 'jittered ones need not' but the jittered case is never run for `unanimity`. The joint property IS tested for `paired_mc_bootstrap` (seed (n) turns `test_the_paired_bootstrap_applies_one_index_to_every_tally` red), so the gap is specific to P5. On a jittered 7-cell synthetic set the committed joint construction gives dissenting=0 / fired=False while the per-cell one gives dissenting=4 / fired=True. Fix: one added test with jittered tallies asserting the two constructions disagree.

--- [3] severity=important
§8.4's launcher-precondition obligation is unenforced: every `need_marker` line can be deleted from the generated `launch.sh` and conformance row L9 plus all 12 launcher/marker/sequence tests stay green (241 passed). §8.4 requires 'A committed test asserts ... that each step's precondition check appears before its command'. The test at /Users/likerun/Desktop/worldcup/epl/tests/test_evwiden.py:4265-4268 asserts `text.index(step) < text.index(command)`, but the marker NAME also appears in the `#   marker: sequence/stepN_*.json` comment inside the preceding block, so the assertion holds vacuously; conformance L9 (/Users/likerun/Desktop/worldcup/epl/evwiden.py:7713-7718) never looks at `need_marker` at all. Mitigation: the harness itself still refuses — `--run`, `--table` and `--merge` each pass `step=` to `require_run_preconditions` → `require_sequence` — so this removes the launcher's early refusal, not the guarantee. Seed (q)'s reorder variant (table before merge) IS caught.

--- [4] severity=important
L12(a)'s committed test goes red for the wrong reason, leaving §3.2's identity control untested at the 62 of 78 openings where it actually fires. Loosening `Engine.fit`'s `np.array_equal(probs_incumbent, stored)` to `worst > 1e-4` (/Users/likerun/Desktop/worldcup/epl/evwiden.py:1927) does turn `test_the_real_engine_fit_refuses_a_difference_no_tolerance_would_see` red — but with `CanaryFailed` raised by the identity-canary branch at epl/evwiden.py:1970-1979, not `ControlMismatch`, because that test's block has an empty `added` set. PROBE with the same seed and a non-empty `added` (evidence {a:0.5, b:50.0}, one corpus probability perturbed by 1e-9): `Engine.fit` ACCEPTS it — control_max_abs_diff = 1.0e-09, identity_canary = None, no refusal. The identity-canary branch only runs where the §2.1 union adds nobody (16 of the 78 openings), so the site §10 names ('The identity control's tolerance is widened after a mismatch, anywhere') is uncovered on the 62 openings that carry treated fixtures. Mitigation: `rejoin`'s merge-level exact re-check (epl/evwiden.py:4150) is covered — seed (b) turns `test_seeded_defect_an_arm_b_that_drifted_from_the_corpus_refuses` red — so a real drift would still be caught at the merge. Fix: a second engine test with a non-empty `added` set.

--- [5] severity=important
§2.3's closure on the frozen constants does not reach `n_sims`, the simulation `seed` or `chunk_size` on any table surface. `TableRunner.__init__` (/Users/likerun/Desktop/worldcup/epl/evwiden.py:4869-4897), `ParityRunner.__init__` (:5113-5116), `run_table` (:5506-5514) and `simulate_arm` (:4561) all accept `n_sims`/`seed`/`chunk_size` and none of them calls `assert_not_overridable` — verified by reading each function's call graph. §2.3 names `n_sims` (20,000) in the closure by name ('The same closure applies to `n_sims` (20,000), `MC_BOOT` (2,000), `SHARDS` (4) and `K` (200)'), and §10 makes 'a second seed ... `n_sims` ... reported as this experiment' an invalidation. Conformance row L18 (:7880-7893) attempts B, MC_BOOT, MC_SEED, replicates, K, shards and two CLI flags — but never `n_sims`. Mitigation: `substantive_digest` carries `n_sims`, `n_particles`, `seed` and `chunk_size` through `plan_state`, so a table leg run at other values fails parity against a default-constructed `ParityRunner`; defeating that needs a second, deliberate override of the oracle too. No CLI flag exposes it.

--- [6] severity=important
A stale `epl-evwiden-2` first-fit record sits in the preregistered run directory, and §8.8's attestation says no such file can exist. /Users/likerun/Desktop/worldcup/data/epl/fit/evwiden/first_real_fit.json (739 bytes, mtime 2026-08-28 15:34) records {"where": "the results canary", "at": "2026-08-28T07:34:17Z", "commit": "6bbacd0", harness evwiden.py f915a5ce…}. §8.8 states 'No `data/epl/fit/evwiden*` or `data/epl/sim/evwiden*` file exists' — literally false as the tree stands, and that sentence is about to be frozen. Substantively it is a test artefact, not a fit: at 6bbacd0 the only module call site is inside `assert_may_fit`'s frozen branch, no freeze block was ever committed so `_frozen_now()` was False and that branch could not run, and 6bbacd0's own test calls `record_first_real_fit(tmp_path, where="the results canary")` — an intermediate working-tree version of that test without `tmp_path` (the parameter was directory-scoped then, defaulting to EVWIDEN_DIR) explains the file exactly; its timestamp is ~90s after 6bbacd0's commit. It is inert now: v2's guard reads `data/epl/fit/evwiden_first_real_fit.json`, a different path, and `first_fit_record()` returns None. No fit artifact exists anywhere and the shared point-in-time store is byte-untouched (184,115 bytes, mtime 2026-08-14 18:41, sha c297b8b0…), consistent with §8.1. It must be deleted and its origin noted before the freeze commit.

--- [7] severity=important
§8.2's authorised pre-freeze pass 4 cannot be executed, and `--freeze-block` renders it among the passes 'actually run'. §8.2 authorises 'One partial engine pass at the first opening (2019-08-09): construction, `fit_points`, the enlarged set, `assert_cutoff_clean` and `assert_point_in_time` — the whole of the fit path except the call to `dcfit.fit_epl`', but `Engine.__init__` calls `assert_may_fit` at /Users/likerun/Desktop/worldcup/epl/evwiden.py:1816, which refuses the pinned archive while unfrozen. VERIFIED: `Engine(corpus, played, directory=<scratch>)` and `Engine(corpus, played)` both raise EvWidenError. `Engine.__init__` also calls `epl_fit.build_store(self.played)` at the default root (:1828), which §8.2's own read-only clause forbids for a pre-freeze pass. Separately, `PRE_FREEZE_RUNS` (:468-495) is a static tuple, so the rendered block enumerates the six authorised passes regardless of what was run, while §8.3 asks for 'the enumeration of every pre-freeze pass actually run, complete'. Either pass 4's text or the guard needs correcting before the block is pasted.

--- [8] severity=minor
`table_gate`'s docstring at /Users/likerun/Desktop/worldcup/epl/evwiden.py:6195-6198 still asserts the superseded, refuted characterisation of P5: '(P5) is deliberately a conservative proxy and is labelled one — ... so the comparison bounds rather than propagates.' §5.4 replaces exactly that with the unanimity rule and demonstrates the proxy invalid ('it does not bound the endpoint by a scale that does not describe it; it propagates the Monte-Carlo uncertainty through the actual computation'). The code implements the unanimity rule correctly — only the deciding function's own docstring, which the freeze commit is about to hash, states the rule the document refutes.

--- [9] severity=minor
Wrong section citations inside the hashed harness. /Users/likerun/Desktop/worldcup/epl/evwiden.py:5853 (`iv_c_verdict`) and :5886 (`unanimity`) cite '§7.3' for the MW6 season-block interval, which is §5.3 — and :5886 sits inside a block quote of §5.4, so the quoted law is misquoted rather than merely mis-referenced. epl/evwiden.py:244 cites '§2.6-equivalent' and v2 has no §2.6. `fit_openings`' docstring at :1270 still says 'the `e* < 12` union', the phrasing §2.3 corrected to 'the union through `e* = 12` (that is, `e* ≤ 12`)'.

--- [10] severity=minor
`harness_freeze_status` accepts `reports/epl_sim_amendments.md` as a freeze source alongside the prereg (/Users/likerun/Desktop/worldcup/epl/evwiden.py:7243) and then checks §8.6 condition (1)'s commit-and-ancestry against whichever source carried the hash table. §8.6 condition (1) names only `reports/epl_widening_prereg_v2.md`, and §8.3 expressly forbids appending an amendment-ledger cross-reference for this document. Low practical risk — the amendments file is protected and append-only, and any block there would still have to carry the schema id and the six membership digests — but the guard does not verify the condition on the file the law names.

--- [11] severity=minor
Two conformance rows are the 'names, not obligations' shape §8.5 condemns, though committed tests carry the behaviour. L11 (/Users/likerun/Desktop/worldcup/epl/evwiden.py:7753-7754) is only the signature pin, while §8.5's own row text also names the equal-digest check; L12 (:7761-7764) is four source-substring checks on `inspect.getsource(Engine.fit)`. Related: §3.3 and §8.5 L11 both say the second sampler-digest test is 'at `TableRunner` level', but it is at `arm_record` level (epl/tests/test_evwiden.py:2661-2700) and `TableRunner.__call__` is still never executed by any test — epl/tests/test_evwiden.py:3935 constructs a `TableRunner` only to assert it refuses. Both seed (o) tests do go red, so the obligation's substance holds.

--- [12] severity=minor
`merge()` still accepts a caller-asserted freeze-state boolean: `harness_frozen: bool | None = None` at /Users/likerun/Desktop/worldcup/epl/evwiden.py:4178, honoured without verification at :4196-4198 ('asserted by caller'). `merge` is not one of §8.6's five named fit surfaces, it fits and simulates nothing, and the flag is not reachable from the CLI — but it is the same 'trusts the Boolean it must establish' shape §8.6 objects to, and it lets a Python caller score a ledger past the freeze check.
